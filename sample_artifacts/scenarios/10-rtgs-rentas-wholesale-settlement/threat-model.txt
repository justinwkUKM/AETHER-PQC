RTGS RENTAS Wholesale Settlement Threat Model Notes

Synthetic organization: Synthetic Central Settlement Participant Lab
Business owner: synthetic-payments-product-owner@example.com
Security owner: synthetic-crypto-architect@example.com
Review date: 2026-06-01

Public research basis:
Inspired by public BNM operational material describing RENTAS as a multi-currency real-time gross settlement system for interbank funds transfer and securities settlement.

Business impact:
Synthetic wholesale RTGS participant gateway inspired by RENTAS concepts: high-value interbank funds transfer, securities settlement, participant terminal access, SWIFT-style messaging, and recovery-site operations.

Risk objective:
Mission-critical wholesale settlement with private network exposure and very high business impact.

Endpoint and control observations:
- rbg.participant.example.com in managed private network reachability=partner protocol=TLS 1.2 crypto=RSA-2048 participant certificate notes=participant terminal access path
- signer.private.participant.example.com in internal reachability=private protocol=HSM private crypto=ECDSA-P256 settlement message signing notes=private signing appliance
- adapter.private.participant.example.com in internal reachability=private protocol=TLS 1.3 crypto=SHA-256 message integrity notes=SWIFT-style message adapter
- liquidity.private.participant.example.com in internal reachability=private protocol=TLS 1.3 crypto=AES-256 queue storage notes=private liquidity queue
- recovery.participant.example.com in recovery site reachability=partner protocol=TLS 1.2 crypto=RSA-3072 recovery token notes=recovery site activation

Expected analyst interpretation:
Even private participant links should rank high because RSA/ECDSA/TLS 1.2/legacy token controls sit on high-value settlement paths; exposure should be PARTNER or INTERNAL rather than public internet.

Owner questions for remediation planning:
- Which team owns certificate, HSM, TLS policy, and signing-profile changes for this payment rail?
- Are the listed protocols active runtime controls, participant compatibility paths, or historical references?
- Is the endpoint public, participant-facing, partner-facing, managed-private-network, private, batch-only, or recovery-only?
- What validation evidence should be uploaded after remediation?

No secret values are present. Any names, domains, owners, and addresses are synthetic placeholders for scanner testing.
